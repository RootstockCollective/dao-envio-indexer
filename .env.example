# Envio Indexer Environment Configuration
# Copy this file to .env.dev, .env.testnet, .env.mainnet, etc.

# Chain Configuration
ENVIO_CHAIN_ID=31
# Start block: Use recent block for fast testing, or 5784028 for full history
# Recent block syncs in minutes, full history takes hours via RPC
ENVIO_START_BLOCK=7290000

# Contract Addresses
ENVIO_GOVERNOR_ADDRESS=0xB1A39B8f57A55d1429324EEb1564122806eb297F

# RPC Configuration (required when HyperSync is not available)
# IMPORTANT: The public node (public-node.testnet.rsk.co) does NOT support eth_getLogs
# Get a free API key at: https://rpc.rootstock.io/
# Format: https://rpc.testnet.rootstock.io/<YOUR_API_KEY>
ENVIO_RPC_URL=https://rpc.testnet.rootstock.io/<YOUR_API_KEY>

# HyperSync API Token (required for HyperSync when available)
# Generate at: https://envio.dev/app/api-tokens
# Note: HyperSync for Rootstock Testnet (chain 31) is pending.
#       Once available, update config.yaml to use hypersync_config.
ENVIO_API_TOKEN=
